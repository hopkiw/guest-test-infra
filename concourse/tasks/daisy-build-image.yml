---
platform: linux

image_resource:
  type: docker-image
  source: {repository: gcr.io/liamh-testing/daisy-with-creds, tag: latest}

inputs:
  - name: compute-image-tools

run:
  path: /daisy
  args: ["-project=liamh-testing","-zone=us-west1-b","./compute-image-tools/daisy-workflows/((wf))"]
